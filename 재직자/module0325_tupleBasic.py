# 튜플 (R) 리스트 (RW)
# 같은 구조  다른 용도
# tuple 타입  : [] 가 아닌 ()
# tuple()

t1 = ()
t2 = (10, )
x = 10 # 정수 상수
z = (10) # 상수에 ()만 사용할 경우 : 연산의 우선순위를 높인다.
y = (10, ) # 하나의 원소를 갖는 튜플은 (첫 원소 , ) 를 사용한다.

# 인덱싱 / 슬라이싱은 리스트 문자열과 동일

30 * 30
#튜플의 메서드 
#t1.count()
#t1.index()

# __add__() , __mul__() 는 사용 가능 
# 튜플에 특정 원소를 추가해야하는 경우
